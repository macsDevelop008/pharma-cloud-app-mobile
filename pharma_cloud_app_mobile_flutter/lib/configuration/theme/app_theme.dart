import 'package:flutter/material.dart';
import 'package:pharma_cloud_app_mobile_flutter/configuration/configuration.dart';

final appTheme = ThemeData.light().copyWith(
  // Define el color por defecto de la app.
  primaryColor: Color(ColorTheme.primaryColor),
  // Define el color de fondo de la app.
  scaffoldBackgroundColor: Color(ColorTheme.backgroundColor),
);
